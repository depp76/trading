# Trading Algorithm — 주간 포트폴리오 업데이트 알고리즘

> 기준일: 2026-08-28 (최초 작성)
> 목적: 주간(금요일 기준) 단위로 Trading Universe 종목을 스코어링하고 포트폴리오 구성을
> 리밸런싱하는 알고리즘을 설계·개발하면서 아이디어와 결정 사항을 계속 누적해 나가는 문서.
> 관련 파일: `data_fetcher.py`(신호/지표 계산), `main.py`의 `TradingRecordTab`(주간 스냅샷),
> `StockTable`(Universe 지표), `ROADMAP.md`(전체 로드맵 — 이 문서는 ROADMAP의 하위 작업 문서)

---

## 목차

1. [배경 및 목표](#1-배경-및-목표)
2. [활용 가능한 기존 코드 자산](#2-활용-가능한-기존-코드-자산)
3. [후보 접근법](#3-후보-접근법)
4. [설계 시 공통 고려사항](#4-설계-시-공통-고려사항)
5. [앱 통합 방안](#5-앱-통합-방안)
6. [검증 계획](#6-검증-계획)
7. [결정 사항 / 진행 상황](#7-결정-사항--진행-상황)
8. [변경 이력](#8-변경-이력)

---

## 1. 배경 및 목표

- 매주(금요일) 단위로 Trading Universe(KOSPI/KOSDAQ, 추후 NASDAQ100/S&P500)를 스캔해
  보유/매수/매도 후보를 기계적으로 산출하는 알고리즘을 만들고자 함.
- `TradingRecordTab`이 이미 금요일 기준으로 자산 스냅샷을 기록하고 있어(`_friday_dates()`),
  이 알고리즘의 실행 주기와 자연스럽게 맞아떨어짐.
- 신호 생성과 실제 매매 실행은 분리한다 — 이 문서의 1차 목표는 "이번 주 기준 매수/매도
  후보 리스트"를 산출하는 것까지이며, 자동 주문 실행 여부는 별도 결정 사항으로 둔다.
- 투자자문이 아닌 개인용 알고리즘 개발 문서 — 실제 자금 투입 전 반드시 백테스트/페이퍼
  트레이딩으로 검증할 것 (6장 참고).

---

## 2. 활용 가능한 기존 코드 자산

`data_fetcher.py`에 이미 구현되어 재사용 가능한 함수/지표:

| 자산 | 위치 | 비고 |
|------|------|------|
| MA10/20/60 이격도, 52주 고저 대비, 기간별 수익률(3/5/10/20/60/120일) | `StockTable` 컬럼 (`fetch_market_data` 등) | Universe 탭에 이미 표시 중 |
| `run_backtest_strategy()` | `data_fetcher.py` | MA 이격도/데드크로스 매도 조건, +30% 익절 조건이 벡터화되어 검증 완료된 상태 |
| `fetch_stock_ma_multi()` | `data_fetcher.py` | 개별 종목 MA10/20/60 시계열 |
| `fetch_investor_trend()` / `_fetch_index_investor_trend()` | `data_fetcher.py` | 기관/외국인 순매수 동향 (Kiwoom 우선, Naver fallback) |
| `fetch_vkospi_history()` / `fetch_vkospi()` | `data_fetcher.py` | 시장 변동성 게이지로 활용 가능 |
| `fetch_quarterly_financials()` | `data_fetcher.py` | 분기 재무 데이터 (밸류 팩터 후보) |
| pykrx `get_market_sector_classifications()` | 미사용 (ROADMAP 4-5) | 섹터 집중도 제약에 필요 |
| `gemini_helper.portfolio_diagnosis()` | `gemini_helper.py` | 산출된 신호에 대한 자연어 해설/리스크 코멘트 생성에 재사용 가능 |

---

## 3. 후보 접근법

### 3-1. 팩터 스코어링 + 순위 리밸런싱

전 종목에 대해 이미 계산되는 지표(PER, MA20/50 이격도, 52주 고저 대비, 기간별 수익률)를
z-score로 정규화해 합산 점수를 매기고, 상위 N개 종목만 보유. 순위가 크게 밀려난 종목은
매도 후보, 새로 상위권에 진입한 종목은 매수 후보로 분류.

- 장점: 기존 컬럼을 그대로 재활용, 팩터 추가/제거가 쉬움
- 핵심 설계 지점: 팩터별 가중치(모멘텀 vs 밸류 vs 퀄리티), 정규화 방식(z-score vs 랭크 백분위)

### 3-2. 모멘텀 로테이션

20~60일 수익률(Chg20D/Chg60D) 상위 K개 종목을 보유, 매주 하위권 탈락 + 신규 상위권 편입.

- 장점: 단순, 백테스트하기 쉬움
- 주의: 회전율(매매 빈도)이 높아짐 → 순위가 일정 폭 이상 밀렸을 때만 교체하는
  밴드(buffer zone) 도입으로 완화 가능

### 3-3. 평균회귀 + 리스크 오버레이

Div20/Div50(이격도)이 낮은(과매도) 종목을 매수 후보로 삼되, VKOSPI를 시장 전체 리스크
게이지로 사용해 VKOSPI 급등 주간에는 신규 매수 비중을 축소.

- 개별 종목 신호(평균회귀)와 시장 국면 신호(VKOSPI)를 분리해서 설계

### 3-4. 기존 백테스트 로직 재사용

`run_backtest_strategy()`의 매수/매도 조건을 개별 종목 시그널 함수로 재활용해, "이번 주
기준으로 조건을 만족하는 종목 목록"을 매주 스캔.

- 이미 벡터화·랜덤 시나리오 500회 검증까지 완료된 로직이라 신규 개발 범위가 가장 작음

---

## 4. 설계 시 공통 고려사항

- **섹터/시장 집중도 제약**: 상위 점수만 뽑으면 특정 업종에 쏠릴 수 있음 —
  `gemini_helper.portfolio_diagnosis()`가 이미 지적하는 "시장별 집중도"처럼 섹터당 최대
  비중 제약 필요. pykrx 섹터 분류(ROADMAP 4-5)와 연계.
- **회전율 관리**: 매주 리밸런싱은 슬리피지·세금 영향이 누적됨 — 밴드 리밸런싱, 최소 보유
  기간 등으로 완화.
- **생존편향**: 현재 KOSPI/KOSDAQ 리스트만 사용하면 상장폐지/편입·편출 이력이 과거
  백테스트에 반영되지 않아 생존편향 발생 가능.
- **포지션 사이징**: 동일가중 vs 변동성 역가중(inverse-vol) vs 기타 방식 결정 필요.
- **과최적화 경계**: 팩터를 늘릴수록 백테스트 성과는 좋아 보이기 쉬우나 실전에서는
  과최적화(overfitting) 위험 — 아웃오브샘플 검증 필수.

---

## 5. 앱 통합 방안

두 갈래 중 선택 필요:

- **(a) 수동 트리거**: `data_fetcher.py`에 `compute_weekly_rebalance_signals(universe_df)`
  함수를 추가하고 Universe 탭에 "주간 리밸런싱 계산" 버튼 추가.
- **(b) 자동 트리거**: `global_auto_timer`처럼 날짜가 금요일로 바뀌는 시점을 감지해 자동
  계산 후 알림(상태바 또는 팝업)만 표시.

신호 생성과 실제 주문 실행은 분리 — 자동 주문 실행 여부는 별도로 결정.

> **(c) 채택안 (2026-08-29)**: 위 (a)/(b) 대신 전용 "Auto Trading" 탭을 신설하고, 그 안에
> "Compute This Week's Signals" 버튼으로 수동 트리거하는 방식으로 구현함(사실상 (a)의
> 변형이나, Universe 탭에 버튼을 얹는 대신 결과 표시용 탭을 통째로 분리). 자동 주문 실행은
> 이번 구현에 포함하지 않음 — 신호 생성까지만.

---

## 6. 검증 계획

1. 워크포워드 백테스트: `run_backtest_strategy`와 동일한 방식으로, 과거 데이터에 대해
   시간순으로 신호를 생성하며 미래 데이터 누수(look-ahead bias) 없는지 확인.
2. 페이퍼 트레이딩: 실제 주문 없이 매주 신호만 기록해 일정 기간 관찰.
3. 소액 실운용: 위 두 단계를 통과한 뒤에만 검토.

---

## 7. 결정 사항 / 진행 상황

> 이 섹션은 논의가 진행되며 계속 채워나갈 부분.

- [x] 접근법 선택 (3장의 3-1~3-4 중) — **3-1 팩터 스코어링 + 순위 리밸런싱** 채택 (2026-08-29)
- [x] 팩터/신호 구성 확정 (v1, 2026-08-29) — 아래 6개 팩터를 동일 가중(단순 평균 z-score)으로
      합산. 백테스트 없이 정한 초기값이므로 6장 검증 후 재조정 예정:
      - `trailing_per` (낮을수록 좋음, 부호 반전)
      - `changes.ma20_div`, `changes.ma50_div` (높을수록 좋음 — 모멘텀)
      - `changes.52w_high_diff` (높을수록/0에 가까울수록 좋음 — 52주 고점 근접도)
      - `changes.20d`, `changes.60d` 수익률 (높을수록 좋음 — 모멘텀)
      - Top N = **20**, 매도 밴드 = Top N × **1.5** = 순위 30위 밖으로 밀려야 매도 후보
        (사용자 결정, 2026-08-29)
      - 팩터 6개 중 3개 미만 유효하면 랭킹에서 제외 (`_REBALANCE_MIN_FACTORS = 3`)
      - 섹터 집중도 제약(4장)은 pykrx 섹터 분류가 아직 미구현(ROADMAP 4-5)이라 이번 v1에는
        미포함 — 추후 과제로 남김
      - 포지션 사이징(4장)은 이번 v1에서 다루지 않음 (신호만 생성, 수량 배분은 미포함)
- [x] 앱 통합 방식 확정 (5장 참고) — 전용 **Auto Trading 탭** 신설 (5장 (c) 참고)
- [ ] 백테스트 결과 기록 — 6장의 워크포워드 백테스트는 아직 미실행. 현재 구현은 "이번 주
      기준 스냅샷 신호"만 계산하며, 과거 시계열에 대한 워크포워드 검증 로직은 별도 구현 필요
- [ ] 페이퍼 트레이딩 시작일 / 결과

**구현 위치** (2026-08-29): `data_fetcher.compute_weekly_rebalance_signals()` (알고리즘),
`ui/auto_trading_tab.py`의 `AutoTradingTab` (UI, "Auto Trading" 탭). 검증: 합성 데이터로
빈 유니버스/지수 제외/데이터 부족 종목 제외/순위 정확성/분산 0 처리/매수·매도·보유 분류·밴드
경계값/`current_holdings=None` 처리를 스크립트로 확인, `trade_db.get_open_trades()` 연동까지
포함한 실제 앱 인스턴스화 스모크 테스트 통과.

---

## 8. 변경 이력

| 날짜 | 변경 내용 |
|------|-----------|
| 2026-08-28 | 최초 작성 — 배경, 기존 코드 자산 정리, 후보 접근법 4가지(팩터 스코어링/모멘텀 로테이션/평균회귀+리스크 오버레이/기존 백테스트 로직 재사용), 공통 고려사항, 앱 통합 방안, 검증 계획 정리 |
| 2026-08-29 | 3-1(팩터 스코어링 + 순위 리밸런싱) 구현. `data_fetcher.compute_weekly_rebalance_signals()`(6개 팩터 동일가중 z-score 합산, Top N=20, 매도 밴드 1.5배=순위 30위) + 전용 `Auto Trading` 탭(`ui/auto_trading_tab.py`) 신설 — Buy/Sell 후보 테이블, 전체 순위 테이블, "Compute This Week's Signals" 버튼. 섹터 집중도 제약·포지션 사이징·워크포워드 백테스트는 이번 범위에서 제외(추후 과제). 검증: 합성 데이터 스크립트(빈 유니버스/지수 제외/데이터 부족 제외/순위 정확성/분산 0/밴드 경계/None 처리) + `trade_db.get_open_trades()` 연동 스모크 테스트, pytest 76/76 통과. |
